di = {
    'a': 5,
    'c': 3,
    'e': 1,
    'b': 4,
    'd': 2
}

print(di)
print(f'Sort by keys = {sorted(di.keys())}')
print(f'Sort by values = {sorted(di.values())}')
print(f'Sort by items = {sorted(di.items())}')

