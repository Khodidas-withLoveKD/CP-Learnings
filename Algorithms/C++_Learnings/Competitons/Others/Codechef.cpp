#include <iostream>
#include <math.h>
#include <vector>
//#include <string>

using namespace std;

int main()
{
    int t,n,l,count,num,slots;

    cin>>t;

    for(int z=0;z<t;z++)
    {
        count=0;

        cin>>n>>l; cin.ignore();

        if(n%2==0)
        {
            for(int sum=0;sum<n;sum=sum+val)
            count++;
            num=1;



        }
        else
            cout<<"0"<<endl;
    }
}
